import { RootState } from ".";

export const actionMiddleware = () => {
    return (store:RootState) => {
        // @ts-ignore: error message 
        return (next) => (action) => {
            
        }
    }

}

